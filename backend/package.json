{
	"name": "housbilling-api",
	"version": "2.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"db:migrate": "wrangler d1 execute housbilling-db --file=./schema.sql",
		"db:migrate:local": "wrangler d1 execute housbilling-db --local --file=./schema.sql"
	},
	"dependencies": {
		"hono": "^4.6.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@cloudflare/workers-types": "^4.20241218.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.58.0"
	}
}